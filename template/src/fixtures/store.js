import { usersMock } from './users';

export const locales = {
  language: 'en',
};

export const startup = {};

export const users = {
  users: usersMock,
};

export const store = {
  locales,
  startup,
  users,
};
