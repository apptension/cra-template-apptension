export enum Color {
  WHITE,
  BLACK,
  BORDER,
  DISABLED,
  PRIMARY,
  SECONDARY,
  ERROR,
}

export enum Font {
  PRIMARY,
}

export enum Border {
  REGULAR,
}

export enum Shadow {
  PRIMARY,
}

export enum Size {
  HEADER,
  CONTENT_HORIZONTAL_PADDING,
  CONTENT_VERTICAL_PADDING,
}

export enum ZIndex {
  CONTENT,
  HEADER,
  OVERLAY,
}
