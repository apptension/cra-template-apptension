import { reducer as {{ camelCase name }}Reducer, INITIAL_STATE } from '../{{ camelCase name }}.redux';

describe('{{ pascalCase name }}: redux', () => {
  const defaultState = INITIAL_STATE;

  describe('reducer', () => {
    it('should return initial state', () => {
      expect({{ camelCase name }}Reducer(undefined, {})).toEqual(defaultState);
    });

    it('should return state on unknown action', () => {
      expect({{ camelCase name }}Reducer(defaultState, { type: 'unknown-action' })).toEqual(defaultState);
    });
  });
});
