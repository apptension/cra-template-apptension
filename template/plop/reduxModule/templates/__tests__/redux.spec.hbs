import { reducer as {{ camelCase name }}Reducer, {{ pascalCase name }}State } from '../{{ camelCase name }}.redux';

describe('{{ pascalCase name }}: redux', () => {
  const defaultState: {{ pascalCase name }}State = {
    {{ camelCase name }}: null,
  };

  describe('reducer', () => {
    it('should return initial state', () => {
      expect({{ camelCase name }}Reducer(undefined, { type: 'unknown-action' })).toEqual(defaultState);
    });

    it('should return state on unknown action', () => {
      expect({{ camelCase name }}Reducer(defaultState, { type: 'unknown-action' })).toEqual(defaultState);
    });
  });
});
