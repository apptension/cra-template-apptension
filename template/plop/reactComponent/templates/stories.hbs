import React from 'react';
import { withKnobs, text } from '@storybook/addon-knobs';

import { {{ pascalCase name }}Component as {{ pascalCase name }} } from './{{ camelCase name }}.component';

const knobs = {
  children: (defaultValue: string) => text('children', defaultValue),
};

const renderComponent = (props = {}) => {
  const defaultProps = {
    children: knobs.children('text'),
  };

  return <{{ pascalCase name }} {...defaultProps} {...props} />;
};

export default {
  title: '{{ pascalCase name }}',
  decorators: [withKnobs],
};

export const Default = () => renderComponent();
export const DifferentTitle = () =>
  renderComponent({
    children: knobs.children('text 2'),
  });
